[package]
name = "agentprep"
version = "0.1.0"
description = "AgentPrep Windows desktop shell"
edition = "2021"

[lib]
name = "agentprep_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
